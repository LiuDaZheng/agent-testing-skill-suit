# 重构指南 - Renew Branch

## 🎯 核心原则

### 铁律：禁止直接精简，只进行合理拆分

**❌ 错误做法：**
- 直接删除内容以减小文件大小
- 简化文档内容
- 移除"不重要"的信息

**✅ 正确做法：**
- 按照文档板块进行逻辑拆分
- 将大文件分解为更小、更专注的文件
- 保持内容完整性，只改变组织结构

## 📋 文件拆分最佳实践

### 1. 按功能模块拆分

```
skill-name/
├── SKILL.md                 # 主入口（保持简洁）
├── docs/
│   ├── README.md           # 项目说明
│   ├── installation.md     # 安装指南
│   ├── usage.md            # 使用文档
│   ├── api-reference.md    # API 参考
│   └── troubleshooting.md  # 故障排除
├── src/
│   ├── index.js            # 入口文件
│   ├── validators/         # 验证器模块
│   ├── generators/         # 生成器模块
│   └── utils/              # 工具函数
└── tests/
    ├── unit/               # 单元测试
    └── integration/        # 集成测试
```

### 2. 按内容类型拆分

**SKILL.md 拆分策略：**
- 保留核心元数据和触发器
- 将详细说明移到 `docs/` 目录
- 将脚本代码移到 `src/` 目录
- 将示例移到 `examples/` 目录

### 3. 文档拆分标准

| 文件类型 | 建议大小 | 拆分触发点 |
|---------|---------|-----------|
| SKILL.md | < 500 行 | 超过 500 行时拆分 |
| README.md | < 1000 行 | 超过 1000 行时拆分 |
| 代码文件 | < 300 行 | 超过 300 行时拆分 |
| 配置文件 | < 200 行 | 超过 200 行时拆分 |

## 🔧 拆分步骤

### 步骤 1: 分析现有内容

```bash
# 检查文件大小
wc -l SKILL.md
wc -l README.md

# 识别主要板块
grep "^## " SKILL.md
```

### 步骤 2: 创建新文件结构

```bash
# 创建目录结构
mkdir -p docs src tests

# 移动内容到对应文件
# (保持内容完整，只改变位置)
```

### 步骤 3: 更新引用

- 更新所有内部链接
- 确保路径正确
- 测试链接有效性

### 步骤 4: 验证

```bash
# 运行 CI/CD 验证
git add .
git commit -m "refactor: 拆分文档内容"
git push origin renew
```

## 📊 拆分检查清单

### 拆分前
- [ ] 已分析文件内容结构
- [ ] 已识别逻辑板块
- [ ] 已规划新文件结构
- [ ] 已备份原始文件

### 拆分后
- [ ] 所有文件 < 建议大小限制
- [ ] 内容完整性已验证
- [ ] 内部链接已更新
- [ ] CI/CD 验证通过
- [ ] 文档可读性提升

## 🚀 CI/CD 验证要求

### 必须通过的检查
1. ✅ YAML Lint 验证
2. ✅ Markdown Lint 验证
3. ✅ 文件结构验证
4. ✅ 安全检查
5. ✅ 文档完整性检查

### 提交要求
- 所有自动化测试必须通过
- 代码审查必须完成
- 文档更新必须完整

## 📝 示例：Skill 文件拆分

### 拆分前
```
SKILL.md (1200 行)
├── 元数据
├── 描述
├── 安装说明
├── 使用指南
├── API 参考
├── 示例代码
└── 故障排除
```

### 拆分后
```
SKILL.md (200 行)           # 仅元数据和核心指令
docs/
├── installation.md (300 行) # 详细安装说明
├── usage.md (400 行)        # 使用指南
├── api-reference.md (200 行) # API 文档
└── examples.md (100 行)     # 示例代码
```

## ⚠️ 注意事项

1. **保持向后兼容**: 如果原有文件被引用，创建重定向或保留入口
2. **更新所有链接**: 确保内部和外部链接都指向正确位置
3. **测试验证**: 每次拆分后运行完整的 CI/CD 流程
4. **文档同步**: 确保所有相关文档都更新

## 📚 参考资源

- [Git 分支管理最佳实践](https://git-scm.com/book/en/v2/Git-Branching)
- [CI/CD 测试自动化指南](https://cloudqa.io/ci-cd-testing-automation-guide/)
- [代码重构最佳实践](https://www.codesee.io/learning-center/code-refactoring)

---

*最后更新：2026-04-12*
*分支：renew*
*状态：活跃开发中*
