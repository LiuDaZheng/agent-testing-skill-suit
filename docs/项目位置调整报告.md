# 项目位置调整报告

> **调整时间**: 2026-04-10 03:36
> **核心规则**: 所有 Skill 开发项目放在 `workspace-skilldev/` 下

---

## ⭐ 核心规则

**重要规则**: **所有 Skill 开发项目必须放在 `~/.openclaw/workspace-skilldev/` 下**

---

## 📁 调整内容

### 调整前（错误）

```
~/.openclaw/
└── projects/                    # ❌ 错误位置
    └── Agent-Testing-Skill-Suit/
```

### 调整后（正确）

```
~/.openclaw/
└── workspace-skilldev/          # ✅ 正确位置
    ├── AGENTS.md
    └── Agent-Testing-Skill-Suit/
        ├── src/skills/
        ├── tests/
        └── docs/
```

---

## 🎯 调整原因

### 用户明确要求

**规则**: 所有 Skill 开发项目放在 `workspace-skilldev/` 下

**原因**:
- ✅ 这是 Agent 的工作区
- ✅ 便于管理和访问
- ✅ 符合 OpenClaw 设计规范

### 内化到 AGENTS.md

**已更新 AGENTS.md**:
```markdown
## ⭐ 核心规则：Skill 开发项目位置

**重要规则**: **所有 Skill 开发项目必须放在 `~/.openclaw/workspace-skilldev/` 下**
```

---

## 📊 影响范围

### 已更新文档

| 文档 | 状态 | 更新内容 |
|------|------|---------|
| AGENTS.md | ✅ 已更新 | 添加核心规则 |
| 项目位置调整报告.md | ✅ 新建 | 记录调整过程 |

### 目录变更

| 原路径 | 新路径 | 状态 |
|--------|--------|------|
| `~/.openclaw/projects/Agent-Testing-Skill-Suit/` | `~/.openclaw/workspace-skilldev/Agent-Testing-Skill-Suit/` | ✅ 已迁移 |
| `~/.openclaw/projects/` | (已删除) | ✅ 已清理 |

---

## ✅ 当前目录结构

```
~/.openclaw/workspace-skilldev/
├── AGENTS.md                    # ⭐ Agent 配置（含核心规则）
├── Agent-Testing-Skill-Suit/    # ⭐ Skill 开发项目
│   ├── README.md
│   ├── DEVELOPMENT.md
│   ├── src/skills/              # Skill 源码
│   ├── tests/                   # 测试
│   └── docs/                    # 项目文档
├── <project-2>/                 # 未来项目
└── <project-3>/                 # 未来项目
```

---

## 📝 多项目管理（在 workspace-skilldev 下）

### 项目创建流程

```bash
# 1. 创建项目目录
mkdir -p ~/.openclaw/workspace-skilldev/<project-name>

# 2. 初始化项目结构
cd ~/.openclaw/workspace-skilldev/<project-name>
mkdir -p src/skills tests docs config scripts logs references

# 3. 创建项目文档
cat > README.md << 'EOF'
# Project Name
项目概述...
EOF
```

### 项目命名规范

- ✅ 使用 kebab-case（短横线分隔）
- ✅ 描述性名称
- ✅ 避免特殊字符

**示例**:
- ✅ `Agent-Testing-Skill-Suit`
- ✅ `feishu-automation`
- ❌ `test skill` (有空格)

---

## 🚨 禁止部署规则（不变）

### 开发中项目

| 位置 | 状态 | 说明 |
|------|------|------|
| `workspace-skilldev/<project>/src/skills/` | ✅ 允许 | 开发目录 |
| `workspace-skilldev/<project>/tests/` | ✅ 允许 | 测试目录 |
| `~/.openclaw/skills/` | ❌ 禁止 | 生产环境 |
| `~/.openclaw/workspace-main/skills/` | ❌ 禁止 | 临时工作区 |

### 已验收项目

| 位置 | 状态 | 说明 |
|------|------|------|
| `~/.openclaw/skills/` | ✅ 允许 | 共享 Skill 目录 |
| `workspace-skilldev/<project>/` | ✅ 保留 | 项目源码保留 |

---

## ✅ 验证清单

- [x] 项目已迁移到 `workspace-skilldev/`
- [x] AGENTS.md 已更新核心规则
- [x] `projects/` 目录已删除
- [x] 辅助 Skill 在 `~/.openclaw/skills/` (正确位置)

---

## 📋 核心规则总结

### ⭐ 必须记住

1. ✅ **所有 Skill 开发项目在 `workspace-skilldev/` 下**
2. ✅ **项目验收后部署到 `~/.openclaw/skills/`**
3. ✅ **禁止部署到 `workspace-main/skills/`**

### 📁 目录职责

| 目录 | 用途 |
|------|------|
| `workspace-skilldev/` | Skill 开发项目 |
| `skills/` | 共享 Skill（已验收） |
| `workspace-main/skills/` | 临时工作区（不使用） |

---

*调整时间：2026-04-10 03:36*
*核心规则：所有 Skill 开发项目在 workspace-skilldev 下*
*状态：已完成，规则已内化到 AGENTS.md*
